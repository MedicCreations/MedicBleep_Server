	<div class="row">
    
        {% if information is defined %}
            <div class="col-md-12">
                <div class="alert alert-success">
                	<button data-dismiss="alert" class="close">
                		×
                	</button>
                	<i class="fa fa-check-circle"></i>
                	{{information}}
                </div>
            </div>
        {% endif %}
        
        {% if error is not defined %}
            <div class="col-md-12 errorHandler no-display">
        		<div class="alert alert-danger">
        			<i class="fa fa-remove-sign"></i>{{lang.users15}}
        		</div>
            </div>
        {% else %}
            <div class="col-md-12 errorHandler">
        		<div class="alert alert-danger">
        			<i class="fa fa-remove-sign"></i> {{error}}
        		</div>
            </div>
        {% endif %}
    
        <!--
		<div class="col-md-3">
			<label>
			    {{lang.users11}}
			</label>
            
            
		    {% if mode == 'add' %}

    			<div class="fileupload-new thumbnail" style="width: 200px; height: 200px;">
    				    <img src="http://www.placehold.it/200x200/EFEFEF/AAAAAA?text=no+image" alt="">
    			</div>
			
			{% endif %}
				
		    {% if mode == 'edit' %}
    			<div class="fileupload-new">
    				<img src="{{FILE_DOWNLOAD_URL}}{{form.image_thumb}}" alt="" class="img-thumbnail">
    			</div>    
			{% endif %}

			<input type="file" name="picture">
			
																							
		</div>
        -->	
        
		<div class="col-md-9">
			
			{% if mode == 'add' %}

        		<div class="form-group">
        			<label class="col-sm-2 control-label" for="form-field-1">
        			{{lang.users6}}
        			</label>
        			<div class="col-sm-9">
        				<input type="text" name="username" {% if mode == 'add' %}placeholder="{{lang.users6}}"{% endif %} id="form-field-1" class="form-control" value="{{form.username}}">
        			</div>
        		</div>
			
    			<div class="form-group">
    				<label class="col-sm-2 control-label" for="form-field-1">
    				    {{lang.users7}}
    				</label>
    				<div class="col-sm-4">
    					<input type="password" name="password" id="input_password" {% if mode == 'add' %}placeholder="{{lang.users7}}"{% endif %} id="form-field-9" class="form-control" value="{{form.password}}">
    				</div>
    				<div class="col-sm-4">
    					<input type="password" name="password_confirm" {% if mode == 'add' %}placeholder="{{lang.users8}}"{% endif %} id="form-field-10" class="form-control" value="{{form.password_confirm}}">
    				</div>
    			</div>
			{% endif %}
			
			<div class="form-group">
				<label class="col-sm-2 control-label" for="form-field-1">
				    {{lang.users12}}
				</label>
				<div class="col-sm-4">
					<input type="text" name="firstname" {% if mode == 'add' %}placeholder="{{lang.users9}}"{% endif %} id="form-field-9" class="form-control" value="{{form.firstname}}">
				</div>
				<div class="col-sm-4">
					<input type="text" name="lastname" {% if mode == 'add' %}placeholder="{{lang.users10}}"{% endif %} id="form-field-10" class="form-control" value="{{form.lastname}}">
				</div>
			</div>
		</div>
	</div>
    
    <input type="hidden" name="id" value="{{form.id}}">
