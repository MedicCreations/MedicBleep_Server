{% extends "adminbase.twig" %}
{% block content %}

<!-- start: PAGE HEADER -->
<div class="row">
	<div class="col-sm-12">

		<!-- start: PAGE TITLE & BREADCRUMB -->
		<ol class="breadcrumb">
			<li>
				<i class="clip-home-3"></i>
				<a href="{{ADMIN_ROOT_URL}}/dashboard">
					{{lang.general11}}
				</a>
			</li>
			<li>
				<a href="{{ADMIN_ROOT_URL}}/groups">
				    {{lang.groups1}}
                </a>
			</li>
			<li class="active">

			    {% if mode == 'view' %}
				    {{lang.groups15}}
				{% endif %}
			    {% if mode == 'delete' %}
				    {{lang.groups24}}
				{% endif %}
				
			</li>
			
			{% include 'searchbox.twig' %}
			
		</ol>
		<div class="row page-header">
		    <div class="col-md-6">
			    <h1>{{data.name}}</h1>
			</div>
		</div>
		<!-- end: PAGE TITLE & BREADCRUMB -->
	</div>
</div>
<!-- end: PAGE HEADER -->
<!-- start: PAGE CONTENT -->

<!-- start: PAGE CONTENT -->
<div class="row">
    
    {% if mode == 'delete' %}
        
        <div class="col-md-12" style="padding-bottom:20px">
    
            <div class="alert alert-warning">
            	<i class="fa fa-exclamation-triangle"></i>
            	<strong>Warning!</strong> 
            	{{lang.groups22}}
            </div>
            
            <form role="form" class="form-horizontal form-user right" method="post" action="{{ADMIN_ROOT_URL}}/groups/delete/{{data.id}}">

                <button type="button" class="btn btn-success" onclick="history.back()">{{lang.general9}}</button>
                <button type="submit" class="btn btn-warning">{{lang.groups29}}</button>

            </form>
            
        </div>
        
    {% endif %}
    		
    <div class="col-md-9">

        <table class="table table-hover" id="sample-table-1">

    		<tr>
    			<td>{{lang.groups17}}</td>
    			<td>{{data.id}}</td>
    		</tr>
    		<tr>
    			<td>{{lang.groups16}}</td>
    			<td>{{data.name}}</td>
    		</tr>
    		<tr>
    			<td>{{lang.category2}}</td>
    			<td>{{categoryList[data.category]}}</td>
    		</tr>
    		<tr>
    			<td>{{lang.groups18}}</td>
    			<td>{{data.created|date("Y.m.d")}}</td>
    		</tr>
    		<tr>
    			<td>{{lang.groups19}}</td>
    			<td>{{data.modified|date("Y.m.d")}}</td>
    		</tr>


        </table>

    </div>

</div>


{% endblock %}
